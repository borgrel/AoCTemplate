package aoc.utils;

public class ArrayTraversal<E extends Enum<E>> {
    /*private E[][] array;

    private ArrayTraversal<E extends Enum<E>>(E[][] input) {
        this.input = input;
    }

    public <E extends Enum<E>> static ArrayTraversal<E> traverse(E[][] array) {
        return new ArrayTraversal<>(array);
    }

     */
}
